# Users API for SociaLink
This API is used to get and post information about users within the SociaLink Application. It is published on Render, which is allowing it to be hosted off of the local computer. 

## Usage
To update Render, push to git and merge with the main branch. To host locally, use npm run dev.